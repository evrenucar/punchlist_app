# Reminders, after the grill: it was never a reminders problem

Parked at your request. This is here so that when you come back to it, the thinking is already done and you are not starting from a blank page.

## The sentence that changed the feature

You picked five answers to "what actually goes wrong when you forget something", and one of them contradicts the whole feature:

> **"I never forget. I just do not start."**

A reminder solves forgetting. You do not forget. Everything else you said points the same way:

- "I lose the block. The work was meant for 2pm and 2pm went somewhere else."
- "I remember late, at 11pm, when I can no longer do it."
- "Too many steps to reach the field." — which is why you have never set one, in a year of using this daily.

So the thing that has been on the roadmap as "reminders" is three different features wearing one coat, and only two of them are worth building.

| What you actually said | What it really is | Verdict |
|---|---|---|
| "A day strip. What is now, what is next, what slipped." | A view | **Build this.** It is the feature. |
| "Yes, daily habits. That is the real missing piece." | Recurrence | **Build this.** Your words: the real missing piece. |
| "One quiet line. Three were due while you were away." | A catch-up line | Build, small, part of the strip |
| An alert that fires at a time | A notification | **Only in-tab, and only after the above** |

## The day strip

Not a list of everything. Three answers to three questions you actually ask, in one line at the top of the board.

```
   NOW              NEXT              SLIPPED
   ───────────────  ────────────────  ─────────────────
   Write the        Call Sam          Renew registration
   release notes    in 40 min         from yesterday
   started 12 min
```

- **NOW** is what you said you would be doing at this moment. It is the answer to "I lose the block".
- **NEXT** is the only thing you need to see coming, and how long you have. It is the answer to "I remember late, at 11pm".
- **SLIPPED** is what was meant for earlier and did not happen. It is the quiet line you asked for, and it replaces the popup you did not want.

The strip is a view over tasks that already have a time. It invents nothing and it stores nothing new.

## Recurrence, which is the part with real weight

You named two shapes and they behave differently:

```
  DAILY HABIT                          STANDING MEETING
  "read 30 minutes"                    "Monday review, 10:00"

  ticking it today must not            it is the same event every week,
  delete tomorrow's                    and skipping one does not
                                       cancel the series
  ─────────────────────────            ─────────────────────────
  one task, many days                  one series, many instances
  history matters (streak)             history barely matters
```

The trap: if a repeating task is one row that you tick, then ticking it destroys the thing that has to come back. Every task app gets this wrong at least once.

The honest model is that a repeating task is a **rule that produces instances**, and the rule is what you edit while the instance is what you tick.

```
   rule:  "read 30 minutes" · every day · 21:00
             │
             ├── Mon 28  [x] done
             ├── Tue 29  [ ] today   ← this is the row on your board
             └── Wed 30      not created yet
```

Only today's instance exists on the board. Yesterday's stays done, tomorrow's appears tomorrow. Ticking never touches the rule.

## What fires, and when

You settled this one cleanly, so it needs no more discussion:

```
   is the board open in a tab?
        │
        ├── yes  →  one quiet in-tab line. No popup, no sound.
        │           "Write the release notes was due 10 minutes ago."
        │
        └── no   →  nothing. You accepted this: "Punchlist only,
                    while open. I accept that it misses things."
                    Real notifications are a later, separate decision
                    and they need an app per platform to be honest.
```

That is a two-line feature, and it only earns its place once the strip exists.

## The one you did not answer

"A task already has a date and a start time. Should the reminder be a third time?" — you said you would rather think about it with a diagram first. Here is the diagram.

```
   TODAY, on a task
   ─────────────────────────────────────────────
   date        29 Jul        when it belongs to
   start       14:00         when you meant to begin
   reminder    ?             when you get poked

   Option A: no third time. The poke IS the start time.
             simplest. "2pm" means both.

   Option B: an offset, not a time. "10 minutes before".
             one number, no new clock to set.

   Option C: a free third time.
             most flexible, most fields, and the reason
             you never set one: "too many steps".
```

Given that the thing stopping you today is the number of steps, B costs one dropdown and A costs nothing.

## Order, when you come back to this

1. **Day shape first.** You already said it: "Nothing breaks today, but I want it back after the day shape work lands." Nothing here works until a task's time is easy to set and easy to see.
2. **The strip.** Now, next, slipped.
3. **Recurrence**, rule and instances, daily first, weekly after.
4. **The quiet in-tab line.** Two lines of code once the strip exists.
5. **Notifications**, only if an app per platform ever happens.

## What I would delete

The current reminder implementation. It has never been used once, it fires only while the tab is open, and it is a third time field reached through too many steps. Keeping it costs a settings row, a field on every task, and a scheduler that runs for nobody. It should go when the strip lands, not before, so nothing is lost in the gap.
